development:
  adapter: postgresql
  database: sample_app-devel
  host: localhost
  min_messages: error

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: sample_app-test
  host: localhost
  min_messages: error

# production:

profile:
  adapter: postgresql
  database: sample_app-devel
  host: localhost
  min_messages: error
